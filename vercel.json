{
  "installCommand": "corepack enable && corepack prepare pnpm@11.3.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@11.3.0 --activate && pnpm --filter @vd-store/web build && node scripts/copy-web-dist.mjs",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
